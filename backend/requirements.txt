# FastAPI + ASGI
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# MQTT
aiomqtt==2.3.0

# Redis
redis[hiredis]==5.2.0

# Pydantic settings
pydantic-settings==2.6.1
pydantic==2.10.3

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Web Push notifications
pywebpush==2.0.0

# Email (password reset, welcome)
aiosmtplib==3.0.1

# Sensor protocol adapters
pymodbus==3.7.4          # Modbus RTU/TCP
pyserial==3.5            # Serial/USB
pyserial-asyncio==0.6    # Async serial
httpx==0.28.0            # HTTP polling adapter
opencv-python-headless==4.10.0.84  # RTSP camera frames

# HTTP FCM push (optional)
aiohttp==3.11.9

# Dev
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0
